{
  "name": "@booltox/plugin-regex-tester",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "ts-node --project backend/tsconfig.json backend/src/server.ts",
    "build:backend": "tsc -p backend/tsconfig.json && mv backend/dist/server.js backend/dist/server.cjs && mv backend/dist/regex-worker.js backend/dist/regex-worker.cjs",
    "build": "pnpm build:backend && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2",
    "vite": "^7.1.7"
  }
}
